# Go Basic Example

Run this example:

```sh
go run main.go
```

Create an API key in the dashboard first, then replace `osp_...` in `main.go`.
Set `OPEN_SPANNER_BASE_URL` when your Open Spanner API is not running at
`http://localhost:18081`.
