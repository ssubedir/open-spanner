# REST Examples

These examples use the REST-backed SDK clients. Start with `basic` to see the
smallest meter and usage flow, then use `advance` for product-shaped metering
scenarios.

| Area | Folder |
| --- | --- |
| Basic SDK flows | `examples/rest/basic` |
| Advanced use cases | `examples/rest/advance` |
