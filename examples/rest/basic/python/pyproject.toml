[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "open-spanner",
]

[tool.uv.sources]
open-spanner = { path = "../../../../sdk/python" }
