// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: open_spanner/v1/usage.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type ClientWritableStream,
  type handleClientStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Value } from "../../google/protobuf/struct.js";
import { Timestamp } from "../../google/protobuf/timestamp.js";

export const protobufPackage = "open_spanner.v1";

export interface UsageEventInput {
  idempotencyKey: string;
  subject: string;
  meter: string;
  quantity: number;
  timestamp: Date | undefined;
  metadata: { [key: string]: any | undefined };
}

export interface UsageEventInput_MetadataEntry {
  key: string;
  value: any | undefined;
}

export interface UsageEvent {
  id: string;
  idempotencyKey: string;
  subject: string;
  meter: string;
  quantity: number;
  timestamp: Date | undefined;
  receivedAt: Date | undefined;
  metadata: { [key: string]: any | undefined };
}

export interface UsageEvent_MetadataEntry {
  key: string;
  value: any | undefined;
}

export interface BulkFailure {
  index: number;
  code: string;
  message: string;
}

export interface CreateUsageRequest {
  event: UsageEventInput | undefined;
}

export interface CreateUsageResponse {
  event: UsageEvent | undefined;
}

export interface CreateUsageBulkRequest {
  idempotencyKey: string;
  events: UsageEventInput[];
}

export interface CreateUsageBulkResponse {
  acceptedCount: number;
  duplicateCount: number;
  failedCount: number;
  accepted: UsageEvent[];
  duplicates: UsageEvent[];
  failed: BulkFailure[];
}

export interface StreamUsageRequest {
  event: UsageEventInput | undefined;
}

export interface StreamUsageResponse {
  acceptedCount: number;
  duplicateCount: number;
  failedCount: number;
  accepted: UsageEvent[];
  duplicates: UsageEvent[];
  failed: BulkFailure[];
}

function createBaseUsageEventInput(): UsageEventInput {
  return { idempotencyKey: "", subject: "", meter: "", quantity: 0, timestamp: undefined, metadata: {} };
}

export const UsageEventInput: MessageFns<UsageEventInput> = {
  encode(message: UsageEventInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.idempotencyKey !== "") {
      writer.uint32(10).string(message.idempotencyKey);
    }
    if (message.subject !== "") {
      writer.uint32(18).string(message.subject);
    }
    if (message.meter !== "") {
      writer.uint32(26).string(message.meter);
    }
    if (message.quantity !== 0) {
      writer.uint32(33).double(message.quantity);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(42).fork()).join();
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, any | undefined]) => {
      if (value !== undefined) {
        UsageEventInput_MetadataEntry.encode({ key: key as any, value }, writer.uint32(50).fork()).join();
      }
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UsageEventInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUsageEventInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.idempotencyKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.subject = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.meter = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.quantity = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = UsageEventInput_MetadataEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.metadata[entry6.key] = entry6.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UsageEventInput {
    return {
      idempotencyKey: isSet(object.idempotencyKey)
        ? globalThis.String(object.idempotencyKey)
        : isSet(object.idempotency_key)
        ? globalThis.String(object.idempotency_key)
        : "",
      subject: isSet(object.subject) ? globalThis.String(object.subject) : "",
      meter: isSet(object.meter) ? globalThis.String(object.meter) : "",
      quantity: isSet(object.quantity) ? globalThis.Number(object.quantity) : 0,
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: any | undefined }, [key, value]: [string, any]) => {
            acc[key] = value as any | undefined;
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: UsageEventInput): unknown {
    const obj: any = {};
    if (message.idempotencyKey !== "") {
      obj.idempotencyKey = message.idempotencyKey;
    }
    if (message.subject !== "") {
      obj.subject = message.subject;
    }
    if (message.meter !== "") {
      obj.meter = message.meter;
    }
    if (message.quantity !== 0) {
      obj.quantity = message.quantity;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, any | undefined][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<UsageEventInput>): UsageEventInput {
    return UsageEventInput.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UsageEventInput>): UsageEventInput {
    const message = createBaseUsageEventInput();
    message.idempotencyKey = object.idempotencyKey ?? "";
    message.subject = object.subject ?? "";
    message.meter = object.meter ?? "";
    message.quantity = object.quantity ?? 0;
    message.timestamp = object.timestamp ?? undefined;
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, any | undefined][]).reduce(
      (acc: { [key: string]: any | undefined }, [key, value]: [string, any | undefined]) => {
        if (value !== undefined) {
          acc[key] = value;
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseUsageEventInput_MetadataEntry(): UsageEventInput_MetadataEntry {
  return { key: "", value: undefined };
}

export const UsageEventInput_MetadataEntry: MessageFns<UsageEventInput_MetadataEntry> = {
  encode(message: UsageEventInput_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      Value.encode(Value.wrap(message.value), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UsageEventInput_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUsageEventInput_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Value.unwrap(Value.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UsageEventInput_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object?.value) ? object.value : undefined,
    };
  },

  toJSON(message: UsageEventInput_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<UsageEventInput_MetadataEntry>): UsageEventInput_MetadataEntry {
    return UsageEventInput_MetadataEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UsageEventInput_MetadataEntry>): UsageEventInput_MetadataEntry {
    const message = createBaseUsageEventInput_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? undefined;
    return message;
  },
};

function createBaseUsageEvent(): UsageEvent {
  return {
    id: "",
    idempotencyKey: "",
    subject: "",
    meter: "",
    quantity: 0,
    timestamp: undefined,
    receivedAt: undefined,
    metadata: {},
  };
}

export const UsageEvent: MessageFns<UsageEvent> = {
  encode(message: UsageEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.idempotencyKey !== "") {
      writer.uint32(18).string(message.idempotencyKey);
    }
    if (message.subject !== "") {
      writer.uint32(26).string(message.subject);
    }
    if (message.meter !== "") {
      writer.uint32(34).string(message.meter);
    }
    if (message.quantity !== 0) {
      writer.uint32(41).double(message.quantity);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(50).fork()).join();
    }
    if (message.receivedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.receivedAt), writer.uint32(58).fork()).join();
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, any | undefined]) => {
      if (value !== undefined) {
        UsageEvent_MetadataEntry.encode({ key: key as any, value }, writer.uint32(66).fork()).join();
      }
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UsageEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUsageEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.idempotencyKey = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.subject = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.meter = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }

          message.quantity = reader.double();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.receivedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          const entry8 = UsageEvent_MetadataEntry.decode(reader, reader.uint32());
          if (entry8.value !== undefined) {
            message.metadata[entry8.key] = entry8.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UsageEvent {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      idempotencyKey: isSet(object.idempotencyKey)
        ? globalThis.String(object.idempotencyKey)
        : isSet(object.idempotency_key)
        ? globalThis.String(object.idempotency_key)
        : "",
      subject: isSet(object.subject) ? globalThis.String(object.subject) : "",
      meter: isSet(object.meter) ? globalThis.String(object.meter) : "",
      quantity: isSet(object.quantity) ? globalThis.Number(object.quantity) : 0,
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      receivedAt: isSet(object.receivedAt)
        ? fromJsonTimestamp(object.receivedAt)
        : isSet(object.received_at)
        ? fromJsonTimestamp(object.received_at)
        : undefined,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: any | undefined }, [key, value]: [string, any]) => {
            acc[key] = value as any | undefined;
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: UsageEvent): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.idempotencyKey !== "") {
      obj.idempotencyKey = message.idempotencyKey;
    }
    if (message.subject !== "") {
      obj.subject = message.subject;
    }
    if (message.meter !== "") {
      obj.meter = message.meter;
    }
    if (message.quantity !== 0) {
      obj.quantity = message.quantity;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.receivedAt !== undefined) {
      obj.receivedAt = message.receivedAt.toISOString();
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, any | undefined][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<UsageEvent>): UsageEvent {
    return UsageEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UsageEvent>): UsageEvent {
    const message = createBaseUsageEvent();
    message.id = object.id ?? "";
    message.idempotencyKey = object.idempotencyKey ?? "";
    message.subject = object.subject ?? "";
    message.meter = object.meter ?? "";
    message.quantity = object.quantity ?? 0;
    message.timestamp = object.timestamp ?? undefined;
    message.receivedAt = object.receivedAt ?? undefined;
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, any | undefined][]).reduce(
      (acc: { [key: string]: any | undefined }, [key, value]: [string, any | undefined]) => {
        if (value !== undefined) {
          acc[key] = value;
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseUsageEvent_MetadataEntry(): UsageEvent_MetadataEntry {
  return { key: "", value: undefined };
}

export const UsageEvent_MetadataEntry: MessageFns<UsageEvent_MetadataEntry> = {
  encode(message: UsageEvent_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      Value.encode(Value.wrap(message.value), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UsageEvent_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUsageEvent_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Value.unwrap(Value.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UsageEvent_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object?.value) ? object.value : undefined,
    };
  },

  toJSON(message: UsageEvent_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<UsageEvent_MetadataEntry>): UsageEvent_MetadataEntry {
    return UsageEvent_MetadataEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UsageEvent_MetadataEntry>): UsageEvent_MetadataEntry {
    const message = createBaseUsageEvent_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? undefined;
    return message;
  },
};

function createBaseBulkFailure(): BulkFailure {
  return { index: 0, code: "", message: "" };
}

export const BulkFailure: MessageFns<BulkFailure> = {
  encode(message: BulkFailure, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).int32(message.index);
    }
    if (message.code !== "") {
      writer.uint32(18).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BulkFailure {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBulkFailure();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BulkFailure {
    return {
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: BulkFailure): unknown {
    const obj: any = {};
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<BulkFailure>): BulkFailure {
    return BulkFailure.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BulkFailure>): BulkFailure {
    const message = createBaseBulkFailure();
    message.index = object.index ?? 0;
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseCreateUsageRequest(): CreateUsageRequest {
  return { event: undefined };
}

export const CreateUsageRequest: MessageFns<CreateUsageRequest> = {
  encode(message: CreateUsageRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.event !== undefined) {
      UsageEventInput.encode(message.event, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateUsageRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateUsageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.event = UsageEventInput.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateUsageRequest {
    return { event: isSet(object.event) ? UsageEventInput.fromJSON(object.event) : undefined };
  },

  toJSON(message: CreateUsageRequest): unknown {
    const obj: any = {};
    if (message.event !== undefined) {
      obj.event = UsageEventInput.toJSON(message.event);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateUsageRequest>): CreateUsageRequest {
    return CreateUsageRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateUsageRequest>): CreateUsageRequest {
    const message = createBaseCreateUsageRequest();
    message.event = (object.event !== undefined && object.event !== null)
      ? UsageEventInput.fromPartial(object.event)
      : undefined;
    return message;
  },
};

function createBaseCreateUsageResponse(): CreateUsageResponse {
  return { event: undefined };
}

export const CreateUsageResponse: MessageFns<CreateUsageResponse> = {
  encode(message: CreateUsageResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.event !== undefined) {
      UsageEvent.encode(message.event, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateUsageResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateUsageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.event = UsageEvent.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateUsageResponse {
    return { event: isSet(object.event) ? UsageEvent.fromJSON(object.event) : undefined };
  },

  toJSON(message: CreateUsageResponse): unknown {
    const obj: any = {};
    if (message.event !== undefined) {
      obj.event = UsageEvent.toJSON(message.event);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateUsageResponse>): CreateUsageResponse {
    return CreateUsageResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateUsageResponse>): CreateUsageResponse {
    const message = createBaseCreateUsageResponse();
    message.event = (object.event !== undefined && object.event !== null)
      ? UsageEvent.fromPartial(object.event)
      : undefined;
    return message;
  },
};

function createBaseCreateUsageBulkRequest(): CreateUsageBulkRequest {
  return { idempotencyKey: "", events: [] };
}

export const CreateUsageBulkRequest: MessageFns<CreateUsageBulkRequest> = {
  encode(message: CreateUsageBulkRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.idempotencyKey !== "") {
      writer.uint32(10).string(message.idempotencyKey);
    }
    for (const v of message.events) {
      UsageEventInput.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateUsageBulkRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateUsageBulkRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.idempotencyKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.events.push(UsageEventInput.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateUsageBulkRequest {
    return {
      idempotencyKey: isSet(object.idempotencyKey)
        ? globalThis.String(object.idempotencyKey)
        : isSet(object.idempotency_key)
        ? globalThis.String(object.idempotency_key)
        : "",
      events: globalThis.Array.isArray(object?.events)
        ? object.events.map((e: any) => UsageEventInput.fromJSON(e))
        : [],
    };
  },

  toJSON(message: CreateUsageBulkRequest): unknown {
    const obj: any = {};
    if (message.idempotencyKey !== "") {
      obj.idempotencyKey = message.idempotencyKey;
    }
    if (message.events?.length) {
      obj.events = message.events.map((e) => UsageEventInput.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<CreateUsageBulkRequest>): CreateUsageBulkRequest {
    return CreateUsageBulkRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateUsageBulkRequest>): CreateUsageBulkRequest {
    const message = createBaseCreateUsageBulkRequest();
    message.idempotencyKey = object.idempotencyKey ?? "";
    message.events = object.events?.map((e) => UsageEventInput.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCreateUsageBulkResponse(): CreateUsageBulkResponse {
  return { acceptedCount: 0, duplicateCount: 0, failedCount: 0, accepted: [], duplicates: [], failed: [] };
}

export const CreateUsageBulkResponse: MessageFns<CreateUsageBulkResponse> = {
  encode(message: CreateUsageBulkResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.acceptedCount !== 0) {
      writer.uint32(8).int32(message.acceptedCount);
    }
    if (message.duplicateCount !== 0) {
      writer.uint32(16).int32(message.duplicateCount);
    }
    if (message.failedCount !== 0) {
      writer.uint32(24).int32(message.failedCount);
    }
    for (const v of message.accepted) {
      UsageEvent.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.duplicates) {
      UsageEvent.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.failed) {
      BulkFailure.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateUsageBulkResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateUsageBulkResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.acceptedCount = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.duplicateCount = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.failedCount = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.accepted.push(UsageEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.duplicates.push(UsageEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.failed.push(BulkFailure.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateUsageBulkResponse {
    return {
      acceptedCount: isSet(object.acceptedCount)
        ? globalThis.Number(object.acceptedCount)
        : isSet(object.accepted_count)
        ? globalThis.Number(object.accepted_count)
        : 0,
      duplicateCount: isSet(object.duplicateCount)
        ? globalThis.Number(object.duplicateCount)
        : isSet(object.duplicate_count)
        ? globalThis.Number(object.duplicate_count)
        : 0,
      failedCount: isSet(object.failedCount)
        ? globalThis.Number(object.failedCount)
        : isSet(object.failed_count)
        ? globalThis.Number(object.failed_count)
        : 0,
      accepted: globalThis.Array.isArray(object?.accepted)
        ? object.accepted.map((e: any) => UsageEvent.fromJSON(e))
        : [],
      duplicates: globalThis.Array.isArray(object?.duplicates)
        ? object.duplicates.map((e: any) => UsageEvent.fromJSON(e))
        : [],
      failed: globalThis.Array.isArray(object?.failed) ? object.failed.map((e: any) => BulkFailure.fromJSON(e)) : [],
    };
  },

  toJSON(message: CreateUsageBulkResponse): unknown {
    const obj: any = {};
    if (message.acceptedCount !== 0) {
      obj.acceptedCount = Math.round(message.acceptedCount);
    }
    if (message.duplicateCount !== 0) {
      obj.duplicateCount = Math.round(message.duplicateCount);
    }
    if (message.failedCount !== 0) {
      obj.failedCount = Math.round(message.failedCount);
    }
    if (message.accepted?.length) {
      obj.accepted = message.accepted.map((e) => UsageEvent.toJSON(e));
    }
    if (message.duplicates?.length) {
      obj.duplicates = message.duplicates.map((e) => UsageEvent.toJSON(e));
    }
    if (message.failed?.length) {
      obj.failed = message.failed.map((e) => BulkFailure.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<CreateUsageBulkResponse>): CreateUsageBulkResponse {
    return CreateUsageBulkResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateUsageBulkResponse>): CreateUsageBulkResponse {
    const message = createBaseCreateUsageBulkResponse();
    message.acceptedCount = object.acceptedCount ?? 0;
    message.duplicateCount = object.duplicateCount ?? 0;
    message.failedCount = object.failedCount ?? 0;
    message.accepted = object.accepted?.map((e) => UsageEvent.fromPartial(e)) || [];
    message.duplicates = object.duplicates?.map((e) => UsageEvent.fromPartial(e)) || [];
    message.failed = object.failed?.map((e) => BulkFailure.fromPartial(e)) || [];
    return message;
  },
};

function createBaseStreamUsageRequest(): StreamUsageRequest {
  return { event: undefined };
}

export const StreamUsageRequest: MessageFns<StreamUsageRequest> = {
  encode(message: StreamUsageRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.event !== undefined) {
      UsageEventInput.encode(message.event, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamUsageRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamUsageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.event = UsageEventInput.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamUsageRequest {
    return { event: isSet(object.event) ? UsageEventInput.fromJSON(object.event) : undefined };
  },

  toJSON(message: StreamUsageRequest): unknown {
    const obj: any = {};
    if (message.event !== undefined) {
      obj.event = UsageEventInput.toJSON(message.event);
    }
    return obj;
  },

  create(base?: DeepPartial<StreamUsageRequest>): StreamUsageRequest {
    return StreamUsageRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StreamUsageRequest>): StreamUsageRequest {
    const message = createBaseStreamUsageRequest();
    message.event = (object.event !== undefined && object.event !== null)
      ? UsageEventInput.fromPartial(object.event)
      : undefined;
    return message;
  },
};

function createBaseStreamUsageResponse(): StreamUsageResponse {
  return { acceptedCount: 0, duplicateCount: 0, failedCount: 0, accepted: [], duplicates: [], failed: [] };
}

export const StreamUsageResponse: MessageFns<StreamUsageResponse> = {
  encode(message: StreamUsageResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.acceptedCount !== 0) {
      writer.uint32(8).int32(message.acceptedCount);
    }
    if (message.duplicateCount !== 0) {
      writer.uint32(16).int32(message.duplicateCount);
    }
    if (message.failedCount !== 0) {
      writer.uint32(24).int32(message.failedCount);
    }
    for (const v of message.accepted) {
      UsageEvent.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.duplicates) {
      UsageEvent.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.failed) {
      BulkFailure.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamUsageResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamUsageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.acceptedCount = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.duplicateCount = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.failedCount = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.accepted.push(UsageEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.duplicates.push(UsageEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.failed.push(BulkFailure.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamUsageResponse {
    return {
      acceptedCount: isSet(object.acceptedCount)
        ? globalThis.Number(object.acceptedCount)
        : isSet(object.accepted_count)
        ? globalThis.Number(object.accepted_count)
        : 0,
      duplicateCount: isSet(object.duplicateCount)
        ? globalThis.Number(object.duplicateCount)
        : isSet(object.duplicate_count)
        ? globalThis.Number(object.duplicate_count)
        : 0,
      failedCount: isSet(object.failedCount)
        ? globalThis.Number(object.failedCount)
        : isSet(object.failed_count)
        ? globalThis.Number(object.failed_count)
        : 0,
      accepted: globalThis.Array.isArray(object?.accepted)
        ? object.accepted.map((e: any) => UsageEvent.fromJSON(e))
        : [],
      duplicates: globalThis.Array.isArray(object?.duplicates)
        ? object.duplicates.map((e: any) => UsageEvent.fromJSON(e))
        : [],
      failed: globalThis.Array.isArray(object?.failed) ? object.failed.map((e: any) => BulkFailure.fromJSON(e)) : [],
    };
  },

  toJSON(message: StreamUsageResponse): unknown {
    const obj: any = {};
    if (message.acceptedCount !== 0) {
      obj.acceptedCount = Math.round(message.acceptedCount);
    }
    if (message.duplicateCount !== 0) {
      obj.duplicateCount = Math.round(message.duplicateCount);
    }
    if (message.failedCount !== 0) {
      obj.failedCount = Math.round(message.failedCount);
    }
    if (message.accepted?.length) {
      obj.accepted = message.accepted.map((e) => UsageEvent.toJSON(e));
    }
    if (message.duplicates?.length) {
      obj.duplicates = message.duplicates.map((e) => UsageEvent.toJSON(e));
    }
    if (message.failed?.length) {
      obj.failed = message.failed.map((e) => BulkFailure.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<StreamUsageResponse>): StreamUsageResponse {
    return StreamUsageResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StreamUsageResponse>): StreamUsageResponse {
    const message = createBaseStreamUsageResponse();
    message.acceptedCount = object.acceptedCount ?? 0;
    message.duplicateCount = object.duplicateCount ?? 0;
    message.failedCount = object.failedCount ?? 0;
    message.accepted = object.accepted?.map((e) => UsageEvent.fromPartial(e)) || [];
    message.duplicates = object.duplicates?.map((e) => UsageEvent.fromPartial(e)) || [];
    message.failed = object.failed?.map((e) => BulkFailure.fromPartial(e)) || [];
    return message;
  },
};

export type UsageServiceService = typeof UsageServiceService;
export const UsageServiceService = {
  createUsage: {
    path: "/open_spanner.v1.UsageService/CreateUsage" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreateUsageRequest): Buffer => Buffer.from(CreateUsageRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateUsageRequest => CreateUsageRequest.decode(value),
    responseSerialize: (value: CreateUsageResponse): Buffer => Buffer.from(CreateUsageResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreateUsageResponse => CreateUsageResponse.decode(value),
  },
  createUsageBulk: {
    path: "/open_spanner.v1.UsageService/CreateUsageBulk" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreateUsageBulkRequest): Buffer =>
      Buffer.from(CreateUsageBulkRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateUsageBulkRequest => CreateUsageBulkRequest.decode(value),
    responseSerialize: (value: CreateUsageBulkResponse): Buffer =>
      Buffer.from(CreateUsageBulkResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreateUsageBulkResponse => CreateUsageBulkResponse.decode(value),
  },
  streamUsage: {
    path: "/open_spanner.v1.UsageService/StreamUsage" as const,
    requestStream: true as const,
    responseStream: false as const,
    requestSerialize: (value: StreamUsageRequest): Buffer => Buffer.from(StreamUsageRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): StreamUsageRequest => StreamUsageRequest.decode(value),
    responseSerialize: (value: StreamUsageResponse): Buffer => Buffer.from(StreamUsageResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): StreamUsageResponse => StreamUsageResponse.decode(value),
  },
} as const;

export interface UsageServiceServer extends UntypedServiceImplementation {
  createUsage: handleUnaryCall<CreateUsageRequest, CreateUsageResponse>;
  createUsageBulk: handleUnaryCall<CreateUsageBulkRequest, CreateUsageBulkResponse>;
  streamUsage: handleClientStreamingCall<StreamUsageRequest, StreamUsageResponse>;
}

export interface UsageServiceClient extends Client {
  createUsage(
    request: CreateUsageRequest,
    callback: (error: ServiceError | null, response: CreateUsageResponse) => void,
  ): ClientUnaryCall;
  createUsage(
    request: CreateUsageRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreateUsageResponse) => void,
  ): ClientUnaryCall;
  createUsage(
    request: CreateUsageRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreateUsageResponse) => void,
  ): ClientUnaryCall;
  createUsageBulk(
    request: CreateUsageBulkRequest,
    callback: (error: ServiceError | null, response: CreateUsageBulkResponse) => void,
  ): ClientUnaryCall;
  createUsageBulk(
    request: CreateUsageBulkRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreateUsageBulkResponse) => void,
  ): ClientUnaryCall;
  createUsageBulk(
    request: CreateUsageBulkRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreateUsageBulkResponse) => void,
  ): ClientUnaryCall;
  streamUsage(
    callback: (error: ServiceError | null, response: StreamUsageResponse) => void,
  ): ClientWritableStream<StreamUsageRequest>;
  streamUsage(
    metadata: Metadata,
    callback: (error: ServiceError | null, response: StreamUsageResponse) => void,
  ): ClientWritableStream<StreamUsageRequest>;
  streamUsage(
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: StreamUsageResponse) => void,
  ): ClientWritableStream<StreamUsageRequest>;
  streamUsage(
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: StreamUsageResponse) => void,
  ): ClientWritableStream<StreamUsageRequest>;
}

export const UsageServiceClient = makeGenericClientConstructor(
  UsageServiceService,
  "open_spanner.v1.UsageService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): UsageServiceClient;
  service: typeof UsageServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
