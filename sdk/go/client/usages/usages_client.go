// Code generated by go-swagger; DO NOT EDIT.

package usages

import (
	"context"
	"fmt"
	"time"

	"github.com/go-openapi/runtime"
	httptransport "github.com/go-openapi/runtime/client"
	"github.com/go-openapi/strfmt"
)

// New creates a new usages API client.
func New(transport runtime.ContextualTransport, formats strfmt.Registry) ClientService {
	return &Client{transport: transport, formats: formats}
}

// New creates a new usages API client with basic auth credentials.
//
// It takes the following parameters:
// - host: http host (github.com).
// - basePath: any base path for the API client ("/v1", "/v3").
// - scheme: http scheme ("http", "https").
// - user: user for basic authentication header.
// - password: password for basic authentication header.
func NewClientWithBasicAuth(host, basePath, scheme, user, password string) ClientService {
	transport := httptransport.New(host, basePath, []string{scheme})
	transport.DefaultAuthentication = httptransport.BasicAuth(user, password)
	return &Client{transport: transport, formats: strfmt.Default}
}

// New creates a new usages API client with a bearer token for authentication.
//
// It takes the following parameters:
// - host: http host (github.com).
// - basePath: any base path for the API client ("/v1", "/v3").
// - scheme: http scheme ("http", "https").
// - bearerToken: bearer token for Bearer authentication header.
func NewClientWithBearerToken(host, basePath, scheme, bearerToken string) ClientService {
	transport := httptransport.New(host, basePath, []string{scheme})
	transport.DefaultAuthentication = httptransport.BearerToken(bearerToken)
	return &Client{transport: transport, formats: strfmt.Default}
}

/*
Client for usages API.
*/
type Client struct {
	transport runtime.ContextualTransport
	formats   strfmt.Registry
}

// ClientOption may be used to customize the behavior of Client methods.
type ClientOption func(*runtime.ClientOperation)

// This client is generated with a few options you might find useful for your swagger spec.
//
// Feel free to add you own set of options.

// WithAccept allows the client to force the Accept header
// to negotiate a specific Producer from the server.
//
// You may use this option to set arbitrary extensions to your MIME media type.
func WithAccept(mime string) ClientOption {
	return func(r *runtime.ClientOperation) {
		r.ProducesMediaTypes = []string{mime}
	}
}

// WithAcceptApplicationJSON sets the Accept header to "application/json".
func WithAcceptApplicationJSON(r *runtime.ClientOperation) {
	r.ProducesMediaTypes = []string{"application/json"}
}

// WithAcceptTextCsv sets the Accept header to "text/csv".
func WithAcceptTextCsv(r *runtime.ClientOperation) {
	r.ProducesMediaTypes = []string{"text/csv"}
}

// ClientService is the interface for Client methods.
type ClientService interface {

	// CreateUsage create usage.
	CreateUsage(params *CreateUsageParams, opts ...ClientOption) (*CreateUsageCreated, error)

	// CreateUsageContext create usage.
	CreateUsageContext(ctx context.Context, params *CreateUsageParams, opts ...ClientOption) (*CreateUsageCreated, error)

	// CreateUsageBulk create usage in bulk.
	CreateUsageBulk(params *CreateUsageBulkParams, opts ...ClientOption) (*CreateUsageBulkCreated, error)

	// CreateUsageBulkContext create usage in bulk.
	CreateUsageBulkContext(ctx context.Context, params *CreateUsageBulkParams, opts ...ClientOption) (*CreateUsageBulkCreated, error)

	// ExportUsageBuckets export usage buckets.
	ExportUsageBuckets(params *ExportUsageBucketsParams, opts ...ClientOption) (*ExportUsageBucketsOK, error)

	// ExportUsageBucketsContext export usage buckets.
	ExportUsageBucketsContext(ctx context.Context, params *ExportUsageBucketsParams, opts ...ClientOption) (*ExportUsageBucketsOK, error)

	// ListIngestionRuns list ingestion runs.
	ListIngestionRuns(params *ListIngestionRunsParams, opts ...ClientOption) (*ListIngestionRunsOK, error)

	// ListIngestionRunsContext list ingestion runs.
	ListIngestionRunsContext(ctx context.Context, params *ListIngestionRunsParams, opts ...ClientOption) (*ListIngestionRunsOK, error)

	// ListUsageBuckets list usage buckets.
	ListUsageBuckets(params *ListUsageBucketsParams, opts ...ClientOption) (*ListUsageBucketsOK, error)

	// ListUsageBucketsContext list usage buckets.
	ListUsageBucketsContext(ctx context.Context, params *ListUsageBucketsParams, opts ...ClientOption) (*ListUsageBucketsOK, error)

	SetTransport(transport runtime.ContextualTransport)
}

/*
CreateUsagecreates usage.

This method does not support injected context.
However, timeout and opentracing contexts are honored whenever enabled.

If you need to pass a specific context, use [Client.CreateUsageContext] instead.
*/
func (a *Client) CreateUsage(params *CreateUsageParams, opts ...ClientOption) (*CreateUsageCreated, error) {
	var ctx context.Context
	if params.inner.ctx != nil {
		ctx = params.inner.ctx
	} else {
		ctx = context.Background()
	}

	return a.CreateUsageContext(ctx, params, opts...)
}

/*
CreateUsageContextcreates usage.

Do not use the deprecated [CreateUsageParams.Context] with this method: it would be ignored.
*/
func (a *Client) CreateUsageContext(ctx context.Context, params *CreateUsageParams, opts ...ClientOption) (*CreateUsageCreated, error) {
	// NOTE: parameters are not validated before sending
	if params == nil {
		params = NewCreateUsageParams()
	}

	op := &runtime.ClientOperation{
		ID:                 "createUsage",
		Method:             "POST",
		PathPattern:        "/v1/usages",
		ProducesMediaTypes: []string{"application/json"},
		ConsumesMediaTypes: []string{"application/json"},
		Schemes:            []string{"http"},
		Params:             params,
		Reader:             &CreateUsageReader{formats: a.formats},
		Client:             params.HTTPClient,
	}

	for _, opt := range opts {
		opt(op)
	}

	result, err := a.transport.SubmitContext(ctx, op)
	if err != nil {
		return nil, err
	}

	// only one success response has to be checked
	success, ok := result.(*CreateUsageCreated)
	if ok {
		return success, nil
	}

	// unexpected success response.

	// no default response is defined.
	//
	// safeguard: normally, in the absence of a default response, unknown success responses return an error above: so this is a codegen issue
	msg := fmt.Sprintf("unexpected success response for createUsage: API contract not enforced by server. Client expected to get an error, but got: %T", result)
	panic(msg)
}

/*
CreateUsageBulkcreates usage in bulk.

This method does not support injected context.
However, timeout and opentracing contexts are honored whenever enabled.

If you need to pass a specific context, use [Client.CreateUsageBulkContext] instead.
*/
func (a *Client) CreateUsageBulk(params *CreateUsageBulkParams, opts ...ClientOption) (*CreateUsageBulkCreated, error) {
	var ctx context.Context
	if params.inner.ctx != nil {
		ctx = params.inner.ctx
	} else {
		ctx = context.Background()
	}

	return a.CreateUsageBulkContext(ctx, params, opts...)
}

/*
CreateUsageBulkContextcreates usage in bulk.

Do not use the deprecated [CreateUsageBulkParams.Context] with this method: it would be ignored.
*/
func (a *Client) CreateUsageBulkContext(ctx context.Context, params *CreateUsageBulkParams, opts ...ClientOption) (*CreateUsageBulkCreated, error) {
	// NOTE: parameters are not validated before sending
	if params == nil {
		params = NewCreateUsageBulkParams()
	}

	op := &runtime.ClientOperation{
		ID:                 "createUsageBulk",
		Method:             "POST",
		PathPattern:        "/v1/usages/bulk",
		ProducesMediaTypes: []string{"application/json"},
		ConsumesMediaTypes: []string{"application/json"},
		Schemes:            []string{"http"},
		Params:             params,
		Reader:             &CreateUsageBulkReader{formats: a.formats},
		Client:             params.HTTPClient,
	}

	for _, opt := range opts {
		opt(op)
	}

	result, err := a.transport.SubmitContext(ctx, op)
	if err != nil {
		return nil, err
	}

	// only one success response has to be checked
	success, ok := result.(*CreateUsageBulkCreated)
	if ok {
		return success, nil
	}

	// unexpected success response.

	// no default response is defined.
	//
	// safeguard: normally, in the absence of a default response, unknown success responses return an error above: so this is a codegen issue
	msg := fmt.Sprintf("unexpected success response for createUsageBulk: API contract not enforced by server. Client expected to get an error, but got: %T", result)
	panic(msg)
}

/*
ExportUsageBucketsexports usage buckets.

This method does not support injected context.
However, timeout and opentracing contexts are honored whenever enabled.

If you need to pass a specific context, use [Client.ExportUsageBucketsContext] instead.
*/
func (a *Client) ExportUsageBuckets(params *ExportUsageBucketsParams, opts ...ClientOption) (*ExportUsageBucketsOK, error) {
	var ctx context.Context
	if params.inner.ctx != nil {
		ctx = params.inner.ctx
	} else {
		ctx = context.Background()
	}

	return a.ExportUsageBucketsContext(ctx, params, opts...)
}

/*
ExportUsageBucketsContextexports usage buckets.

Do not use the deprecated [ExportUsageBucketsParams.Context] with this method: it would be ignored.
*/
func (a *Client) ExportUsageBucketsContext(ctx context.Context, params *ExportUsageBucketsParams, opts ...ClientOption) (*ExportUsageBucketsOK, error) {
	// NOTE: parameters are not validated before sending
	if params == nil {
		params = NewExportUsageBucketsParams()
	}

	op := &runtime.ClientOperation{
		ID:                 "exportUsageBuckets",
		Method:             "GET",
		PathPattern:        "/v1/usages/export",
		ProducesMediaTypes: []string{"text/csv"},
		ConsumesMediaTypes: []string{"application/json"},
		Schemes:            []string{"http"},
		Params:             params,
		Reader:             &ExportUsageBucketsReader{formats: a.formats},
		Client:             params.HTTPClient,
	}

	for _, opt := range opts {
		opt(op)
	}

	result, err := a.transport.SubmitContext(ctx, op)
	if err != nil {
		return nil, err
	}

	// only one success response has to be checked
	success, ok := result.(*ExportUsageBucketsOK)
	if ok {
		return success, nil
	}

	// unexpected success response.

	// no default response is defined.
	//
	// safeguard: normally, in the absence of a default response, unknown success responses return an error above: so this is a codegen issue
	msg := fmt.Sprintf("unexpected success response for exportUsageBuckets: API contract not enforced by server. Client expected to get an error, but got: %T", result)
	panic(msg)
}

/*
ListIngestionRunslists ingestion runs.

This method does not support injected context.
However, timeout and opentracing contexts are honored whenever enabled.

If you need to pass a specific context, use [Client.ListIngestionRunsContext] instead.
*/
func (a *Client) ListIngestionRuns(params *ListIngestionRunsParams, opts ...ClientOption) (*ListIngestionRunsOK, error) {
	var ctx context.Context
	if params.inner.ctx != nil {
		ctx = params.inner.ctx
	} else {
		ctx = context.Background()
	}

	return a.ListIngestionRunsContext(ctx, params, opts...)
}

/*
ListIngestionRunsContextlists ingestion runs.

Do not use the deprecated [ListIngestionRunsParams.Context] with this method: it would be ignored.
*/
func (a *Client) ListIngestionRunsContext(ctx context.Context, params *ListIngestionRunsParams, opts ...ClientOption) (*ListIngestionRunsOK, error) {
	// NOTE: parameters are not validated before sending
	if params == nil {
		params = NewListIngestionRunsParams()
	}

	op := &runtime.ClientOperation{
		ID:                 "listIngestionRuns",
		Method:             "GET",
		PathPattern:        "/v1/usageingestions",
		ProducesMediaTypes: []string{"application/json"},
		ConsumesMediaTypes: []string{"application/json"},
		Schemes:            []string{"http"},
		Params:             params,
		Reader:             &ListIngestionRunsReader{formats: a.formats},
		Client:             params.HTTPClient,
	}

	for _, opt := range opts {
		opt(op)
	}

	result, err := a.transport.SubmitContext(ctx, op)
	if err != nil {
		return nil, err
	}

	// only one success response has to be checked
	success, ok := result.(*ListIngestionRunsOK)
	if ok {
		return success, nil
	}

	// unexpected success response.

	// no default response is defined.
	//
	// safeguard: normally, in the absence of a default response, unknown success responses return an error above: so this is a codegen issue
	msg := fmt.Sprintf("unexpected success response for listIngestionRuns: API contract not enforced by server. Client expected to get an error, but got: %T", result)
	panic(msg)
}

/*
ListUsageBucketslists usage buckets.

This method does not support injected context.
However, timeout and opentracing contexts are honored whenever enabled.

If you need to pass a specific context, use [Client.ListUsageBucketsContext] instead.
*/
func (a *Client) ListUsageBuckets(params *ListUsageBucketsParams, opts ...ClientOption) (*ListUsageBucketsOK, error) {
	var ctx context.Context
	if params.inner.ctx != nil {
		ctx = params.inner.ctx
	} else {
		ctx = context.Background()
	}

	return a.ListUsageBucketsContext(ctx, params, opts...)
}

/*
ListUsageBucketsContextlists usage buckets.

Do not use the deprecated [ListUsageBucketsParams.Context] with this method: it would be ignored.
*/
func (a *Client) ListUsageBucketsContext(ctx context.Context, params *ListUsageBucketsParams, opts ...ClientOption) (*ListUsageBucketsOK, error) {
	// NOTE: parameters are not validated before sending
	if params == nil {
		params = NewListUsageBucketsParams()
	}

	op := &runtime.ClientOperation{
		ID:                 "listUsageBuckets",
		Method:             "GET",
		PathPattern:        "/v1/usages",
		ProducesMediaTypes: []string{"application/json"},
		ConsumesMediaTypes: []string{"application/json"},
		Schemes:            []string{"http"},
		Params:             params,
		Reader:             &ListUsageBucketsReader{formats: a.formats},
		Client:             params.HTTPClient,
	}

	for _, opt := range opts {
		opt(op)
	}

	result, err := a.transport.SubmitContext(ctx, op)
	if err != nil {
		return nil, err
	}

	// only one success response has to be checked
	success, ok := result.(*ListUsageBucketsOK)
	if ok {
		return success, nil
	}

	// unexpected success response.

	// no default response is defined.
	//
	// safeguard: normally, in the absence of a default response, unknown success responses return an error above: so this is a codegen issue
	msg := fmt.Sprintf("unexpected success response for listUsageBuckets: API contract not enforced by server. Client expected to get an error, but got: %T", result)
	panic(msg)
}

// SetTransport changes the transport on the client
func (a *Client) SetTransport(transport runtime.ContextualTransport) {
	a.transport = transport
}

// innerParams captures internal fields so they don't conflict with user-supplied parameters.
type innerParams struct {
	timeout time.Duration

	// Deprecated: use the operation call with context to pass the context instead of [UsagesParams].
	ctx context.Context
}
