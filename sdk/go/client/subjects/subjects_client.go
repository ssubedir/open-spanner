// Code generated by go-swagger; DO NOT EDIT.

package subjects

import (
	"context"
	"fmt"
	"time"

	"github.com/go-openapi/runtime"
	httptransport "github.com/go-openapi/runtime/client"
	"github.com/go-openapi/strfmt"
)

// New creates a new subjects API client.
func New(transport runtime.ContextualTransport, formats strfmt.Registry) ClientService {
	return &Client{transport: transport, formats: formats}
}

// New creates a new subjects API client with basic auth credentials.
//
// It takes the following parameters:
// - host: http host (github.com).
// - basePath: any base path for the API client ("/v1", "/v3").
// - scheme: http scheme ("http", "https").
// - user: user for basic authentication header.
// - password: password for basic authentication header.
func NewClientWithBasicAuth(host, basePath, scheme, user, password string) ClientService {
	transport := httptransport.New(host, basePath, []string{scheme})
	transport.DefaultAuthentication = httptransport.BasicAuth(user, password)
	return &Client{transport: transport, formats: strfmt.Default}
}

// New creates a new subjects API client with a bearer token for authentication.
//
// It takes the following parameters:
// - host: http host (github.com).
// - basePath: any base path for the API client ("/v1", "/v3").
// - scheme: http scheme ("http", "https").
// - bearerToken: bearer token for Bearer authentication header.
func NewClientWithBearerToken(host, basePath, scheme, bearerToken string) ClientService {
	transport := httptransport.New(host, basePath, []string{scheme})
	transport.DefaultAuthentication = httptransport.BearerToken(bearerToken)
	return &Client{transport: transport, formats: strfmt.Default}
}

/*
Client for subjects API.
*/
type Client struct {
	transport runtime.ContextualTransport
	formats   strfmt.Registry
}

// ClientOption may be used to customize the behavior of Client methods.
type ClientOption func(*runtime.ClientOperation)

// ClientService is the interface for Client methods.
type ClientService interface {

	// ListSubjectUsageEvents list subject usage events.
	ListSubjectUsageEvents(params *ListSubjectUsageEventsParams, opts ...ClientOption) (*ListSubjectUsageEventsOK, error)

	// ListSubjectUsageEventsContext list subject usage events.
	ListSubjectUsageEventsContext(ctx context.Context, params *ListSubjectUsageEventsParams, opts ...ClientOption) (*ListSubjectUsageEventsOK, error)

	// ListSubjects list subjects.
	ListSubjects(params *ListSubjectsParams, opts ...ClientOption) (*ListSubjectsOK, error)

	// ListSubjectsContext list subjects.
	ListSubjectsContext(ctx context.Context, params *ListSubjectsParams, opts ...ClientOption) (*ListSubjectsOK, error)

	SetTransport(transport runtime.ContextualTransport)
}

/*
ListSubjectUsageEventslists subject usage events.

This method does not support injected context.
However, timeout and opentracing contexts are honored whenever enabled.

If you need to pass a specific context, use [Client.ListSubjectUsageEventsContext] instead.
*/
func (a *Client) ListSubjectUsageEvents(params *ListSubjectUsageEventsParams, opts ...ClientOption) (*ListSubjectUsageEventsOK, error) {
	var ctx context.Context
	if params.inner.ctx != nil {
		ctx = params.inner.ctx
	} else {
		ctx = context.Background()
	}

	return a.ListSubjectUsageEventsContext(ctx, params, opts...)
}

/*
ListSubjectUsageEventsContextlists subject usage events.

Do not use the deprecated [ListSubjectUsageEventsParams.Context] with this method: it would be ignored.
*/
func (a *Client) ListSubjectUsageEventsContext(ctx context.Context, params *ListSubjectUsageEventsParams, opts ...ClientOption) (*ListSubjectUsageEventsOK, error) {
	// NOTE: parameters are not validated before sending
	if params == nil {
		params = NewListSubjectUsageEventsParams()
	}

	op := &runtime.ClientOperation{
		ID:                 "listSubjectUsageEvents",
		Method:             "GET",
		PathPattern:        "/v1/subjects/{subject}/usageevents",
		ProducesMediaTypes: []string{"application/json"},
		ConsumesMediaTypes: []string{"application/json"},
		Schemes:            []string{"http"},
		Params:             params,
		Reader:             &ListSubjectUsageEventsReader{formats: a.formats},
		Client:             params.HTTPClient,
	}

	for _, opt := range opts {
		opt(op)
	}

	result, err := a.transport.SubmitContext(ctx, op)
	if err != nil {
		return nil, err
	}

	// only one success response has to be checked
	success, ok := result.(*ListSubjectUsageEventsOK)
	if ok {
		return success, nil
	}

	// unexpected success response.

	// no default response is defined.
	//
	// safeguard: normally, in the absence of a default response, unknown success responses return an error above: so this is a codegen issue
	msg := fmt.Sprintf("unexpected success response for listSubjectUsageEvents: API contract not enforced by server. Client expected to get an error, but got: %T", result)
	panic(msg)
}

/*
ListSubjectslists subjects.

This method does not support injected context.
However, timeout and opentracing contexts are honored whenever enabled.

If you need to pass a specific context, use [Client.ListSubjectsContext] instead.
*/
func (a *Client) ListSubjects(params *ListSubjectsParams, opts ...ClientOption) (*ListSubjectsOK, error) {
	var ctx context.Context
	if params.inner.ctx != nil {
		ctx = params.inner.ctx
	} else {
		ctx = context.Background()
	}

	return a.ListSubjectsContext(ctx, params, opts...)
}

/*
ListSubjectsContextlists subjects.

Do not use the deprecated [ListSubjectsParams.Context] with this method: it would be ignored.
*/
func (a *Client) ListSubjectsContext(ctx context.Context, params *ListSubjectsParams, opts ...ClientOption) (*ListSubjectsOK, error) {
	// NOTE: parameters are not validated before sending
	if params == nil {
		params = NewListSubjectsParams()
	}

	op := &runtime.ClientOperation{
		ID:                 "listSubjects",
		Method:             "GET",
		PathPattern:        "/v1/subjects",
		ProducesMediaTypes: []string{"application/json"},
		ConsumesMediaTypes: []string{"application/json"},
		Schemes:            []string{"http"},
		Params:             params,
		Reader:             &ListSubjectsReader{formats: a.formats},
		Client:             params.HTTPClient,
	}

	for _, opt := range opts {
		opt(op)
	}

	result, err := a.transport.SubmitContext(ctx, op)
	if err != nil {
		return nil, err
	}

	// only one success response has to be checked
	success, ok := result.(*ListSubjectsOK)
	if ok {
		return success, nil
	}

	// unexpected success response.

	// no default response is defined.
	//
	// safeguard: normally, in the absence of a default response, unknown success responses return an error above: so this is a codegen issue
	msg := fmt.Sprintf("unexpected success response for listSubjects: API contract not enforced by server. Client expected to get an error, but got: %T", result)
	panic(msg)
}

// SetTransport changes the transport on the client
func (a *Client) SetTransport(transport runtime.ContextualTransport) {
	a.transport = transport
}

// innerParams captures internal fields so they don't conflict with user-supplied parameters.
type innerParams struct {
	timeout time.Duration

	// Deprecated: use the operation call with context to pass the context instead of [SubjectsParams].
	ctx context.Context
}
