---
title: Your First Query
description: Query bucketed usage for one subject and meter.
---

Usage queries return buckets. A bucket is a time window with a meter aggregation applied.

This query returns daily `api_requests` usage for `org_123`:

```sh
curl "http://localhost:18081/v1/usages?subject=org_123&meter=api_requests&from=2026-06-01T00:00:00Z&to=2026-06-10T00:00:00Z&bucket_size=day" \
  -H "Authorization: Bearer $OPEN_SPANNER_API_KEY"
```

Use this shape when you already know the subject, meter, time window, and bucket size.

For richer filters or grouping, use `POST /v1/usages/search` or the dashboard usage page.
